query GetAllSurahs($pagination: PaginationInput!) {
  getAllSurahs(pagination: $pagination) {
    surahs {
      id
      number
      uzbekName
      uzbekNameTranslation
      qfcName
      revelationType
      totalAyahs
    }
    hasMore
  }
}
